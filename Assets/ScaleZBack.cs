using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScaleZBack : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //this.transform.
      //  iTween.ScaleTo(this.gameObject, new Vector3(this.transform.localScale.x, this.transform.localScale.y, 0),animationTiome);
	}
	
}
